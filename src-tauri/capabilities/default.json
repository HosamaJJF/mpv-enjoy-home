{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions required by the main media library window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
